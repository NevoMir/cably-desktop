# Changes from upstream KiCad (GPLv3 §5(a) record)

Base: KiCad tag 10.0.6 (gitlab.com/kicad/code/kicad).

| date | area | change |
|---|---|---|
| 2026-09-02 | repo | Forked at 10.0.6; added NOTICE.md, CHANGES.md, `cably/` for additions; upstream remote kept for rebasing. No source files modified yet. |
| 2026-09-03 | theme | Added `cably/themes/Cably.json` and `Cably Dark.json` (181 colour keys, schema 5, generated from cably.dev's palette) and `cably/tests/theme.sh` (key completeness against color_settings.cpp + kicad-cli render oracle with negative control). Data only; no source changes. |
| 2026-09-03 | toolchain | `cably/toolchain/`: the kicad-mac-builder patch (ngspice clang flag, 3D-models step no-op, conditional docs copy, tolerant ad-hoc signing) and the exact macOS 26 build recipe. F1 acceptance passed: app launches; Gerbers/netlist match the official 10.0.1 build. No KiCad source changes. |
| 2026-09-03 | icons | Replaced KiCad's application and document icons with Cably's own family (a rounded tile in Cably blue carrying a cable-shaped "C" soldered into two pads; per-editor accent badges; paper+mark document icons). Sources: `cably/icons/make-svgs.py` -> `cably/icons/src/*.svg` (18); `cably/icons/build-icns.sh` renders them with rsvg-convert (qlmanage fallback) and packs with iconutil, overwriting the 18 `*.icns` under kicad/, pcbnew/, eeschema/, gerbview/, pagelayout_editor/, pcb_calculator/, bitmap2component/, cvpcb/, the in-app logo bitmaps `resources/bitmaps_png/png/{icon_kicad*,project_kicad*}.png` (48, used by the About dialog, window icons and the project tree), their `sources/{light,dark}/*.svg`, and `resources/bitmaps_png/icons/icon_kicad{,_64}.png` + `icon_kicad{,_nightly}.ico` (PNG-entry ICO). `cably/tests/icons.sh` proves the tree is byte-identical to a fresh regeneration, that each icns round-trips through iconutil with 16..1024 slices showing the mark, and that every file differs from upstream 10.0.6. No C++/CMake changes; `resources/bitmaps_png/icons/icon_kicad*.xpm` (unused by the build) and `sources/*/kicad_logo*.svg` (unreferenced) still carry KiCad's artwork. |
| 2026-09-03 | home | F3(d): Cably home screen. New `cably/src/cably_home_panel.{h,cpp}` (wxPanel: title, "Describe the circuit you want" prompt with a disabled Generate button — no network until the cloud bridge — recent projects from the manager's FILE_HISTORY, Open project via `KICAD_MANAGER_ACTIONS::openProject`, footer opening About via `ACTIONS::about`) and `cably/src/cably_home_recent.h` (pure helper + `cably/tests/unit/test_cably_home_recent.cpp`). New `cably/CMakeLists.txt` seam (target_sources on `kicad`). MODIFIED `kicad/kicad_manager_frame.{h,cpp}`: the home panel is a second wxAUI centre pane; `updateCablyHomeVisibility()` shows it while no project is loaded and swaps back to the project tree + editors notebook once one is (called from the constructor, LoadProject, CloseProject); tree width only persisted while shown; language change forwarded. Acceptance: `cably/tests/home.sh`. |
| 2026-09-03 | identity | F2 — the product identifies as **Cably Desktop, based on KiCad**. `resources/macos/plist/*.Info.plist.in` (7): CFBundleIdentifier `org.kicad.*` → `org.cably.desktop` / `org.cably.desktop.{pcbnew,eeschema,gerbview,pcb-calculator,pl-editor,bitmap2component}`, CFBundleName + new CFBundleDisplayName "Cably Desktop" / "Cably PCB Editor" / "Cably Schematic Editor" / "Cably Gerber Viewer" / "Cably PCB Calculator" / "Cably Drawing Sheet Editor" / "Cably Image Converter", CFBundleGetInfoString "Cably Desktop, based on KiCad <version>", NSHumanReadableCopyright now names both The KiCad Developers and Cably. `MACOSX_BUNDLE_GUI_IDENTIFIER` aligned in kicad/, pcbnew/, eeschema/, gerbview/, pcb_calculator/, pagelayout_editor/, bitmap2component/ CMakeLists.txt (GPL header added to those seven files). `kicad/CMakeLists.txt`: top-level launcher symlinks renamed to the "Cably …" names above and a `Cably Desktop.app` symlink added; the bundle directory stays `KiCad.app` (kicad-mac-builder addresses it by that path; F6 packaging presents the product name). `kicad/kicad.cpp`: wxApp display name and manager frame title → CABLY_PRODUCT_NAME. `kicad/kicad_manager_frame.cpp`: About title and both window-title sites "KiCad [x.y]" → "Cably Desktop" (no version in the title: the version is KiCad's and is attributed in About). About titles `_HKI( "KiCad <Editor>" )` → `"Cably <Editor>"` in pcbnew (pcb_edit, footprint_edit, footprint_viewer), eeschema (sch_edit, symbol_viewer, symbol_edit), gerbview, pagelayout_editor, pcb_calculator, bitmap2component frames (msgids changed; these 10 strings fall back to English until re-translated). `common/dialog_about/AboutDialog_main.cpp`: description now "Cably Desktop … based on KiCad"; new "Cably Desktop" section with "based on KiCad — Copyright © The KiCad Developers", Source code (CABLY_SOURCE_URL), Cably website, Cably bug link; KiCad website/dev/library links kept as attribution; KiCad bug-tracker and forum links removed (fork issues must not go to KiCad) and a trademark/non-affiliation note added; license page keeps the GPL-3.0-or-later notice and adds the corresponding-source URL and the copyright split. New: `cably/src/cably_config.h` (CABLY_PRODUCT_NAME, CABLY_BASED_ON, CABLY_WEBSITE_URL, CABLY_SOURCE_URL + CABLY_BUGS_URL — PLACEHOLDER URLs, F7 must set the real GPLv3 §6 source location before distribution), top-level `include( cably/CMakeLists.txt )` seam + F2 block in it, `cably/tests/identity.sh` (asserts bundle ids/names on the built app, About strings in the linked binaries, launcher symlinks; optional live-launch check). Not touched: internal identifiers, translation keys other than the 10 About titles, file extensions/type names, kicad-cli, icons, vendor name for config paths. |
| 2026-09-03 | toolchain | Build agent: `cably/toolchain/kicad-mac-builder-macos26.patch` was never in git (upstream `.gitignore` ignores `*.patch`; force-added) and the live `kicad-mac-builder/kicad.cmake` it captured was corrupted after the F1 build (the first 122 lines pasted a second time inside the `if(DEFINED REDISTRIBUTABLE)` block, leaving it unclosed, and the sign-app step was NOT tolerant despite the README). Regenerated from upstream + the three intended hunks: conditional docs copy, packages3d no-op, and sign-app wrapped in `sh -c "... || echo"` so the macOS 26 ad-hoc codesign failure on the embedded Python.framework no longer aborts the build. README: how to force an incremental rebuild (ExternalProject stamps). No KiCad source changes. |
| 2026-09-03 | home | Build agent: `cably/tests/home.sh` check (a) gave false FAILs (`set -o pipefail` + `grep -q` exiting early -> `strings` SIGPIPE -> non-zero pipeline status); now `grep -F >/dev/null`. First green run on the rebuilt app. |
| 2026-09-03 | identity | Reworded a comment in cably/src/cably_config.h so the trademark check (a `git grep` for the product name followed by the KiCad mark must be empty) holds. |
| 2026-09-03 | home | F4 (agent home-f4): the home panel talks to the cloud bridge. `cably/src/cably_home_panel.{h,cpp}` rewritten around two visible paths: (1) the prompt's **Generate** now opens `https://cably.dev/app?prompt=<percent-encoded>` in the system browser (tooltip "Generates on cably.dev; open the result here afterwards"; generation is NOT orchestrated by the fork); (2) a "Your Cably projects" card: **Sign in to Cably** starts the bridge's 127.0.0.1 loopback, opens the hand-off URL, shows "Waiting for the browser…" with Cancel; on success "Signed in as <email>", the session goes to the keychain through the bridge and the user's projects are listed newest first (name + "updated 5 min ago"/date) with an **Open** button each: FetchProject → ExportProject (engine.cably.dev) → WriteProjectFolder under `~/Documents/Cably Desktop/<stem>/` → `KICAD_MANAGER_FRAME::LoadProject`. A conflict (files KiCad edited since the last export) asks "This project was edited here since Cably last exported it." with Keep / Replace, mirroring the web app's desktop button. Sign out clears the keychain entry via the bridge; a stored session is validated at start-up and dropped with a reason if dead. Every bridge call runs on a detached thread, results return via CallAfter behind a liveness token + serial (cancel/sign-out discard stale results). The bridge (`cably/src/cably_bridge.h`, one `CABLY_BRIDGE` over `CABLY_HTTP_KICAD` + `CABLY_KEYCHAIN_SECRET_STORE(CABLY_KEYCHAIN_SERVICE)`, single-threaded by design) is owned by the panel behind a mutex and reached ONLY through the adapter section at the top of cably_home_panel.cpp; the sign-in wait is sliced (`CABLY_LOOPBACK_SERVER::WaitForSession` 500 ms at a time, 10 min cap) so Cancel is a flag and the waiting worker stops the listener itself — no loopback is destroyed under a thread blocked on it; success calls `FinishLoopback(0)` (persists + stops). `WriteProjectFolder` is the bridge's static one (root = ~/Documents/Cably Desktop, it applies SafeStem and the .cably-export.json rule); a 401-cleared session (`!HasSession()`) drops the UI to signed-out with the reason. Verified: the panel TU passes `-fsyntax-only` with the build's own flags against the landed bridge header. New `cably/src/cably_home_cloud.h` (pure: CablyProjectStem mirrors the web app's safeProjectFileStem; CablyParseIsoUtc; CablyFormatUpdated; CablyPercentEncode; CablyGenerateUrl) + `cably/tests/unit/test_cably_home_cloud.cpp` (47 checks, green). `cably/tests/home.sh`: asserts the new strings in the binary, that the F3 placeholder "coming in the next phase" is gone, and builds/runs both unit tests. `cably/CMakeLists.txt`: F4 block. Only the public website URL (CABLY_WEBSITE_URL) is named here; Supabase/engine endpoints live in the bridge. |
| 2026-09-03 | bridge | F4: cloud bridge (C++, no GUI yet). New `cably/src/cably_bridge.{h,cpp}`: pure C++17 core (nlohmann/json + picosha2 headers, POSIX sockets) — `CABLY_LOOPBACK_SERVER` (sign-in handoff: binds 127.0.0.1:<kernel port>, 32-byte hex `state`; `GET /callback` serves a page whose script reads `location.hash` and POSTs it as JSON to `/token` on the same origin; `POST /token` accepted only when `state` matches, exactly once, then the listener closes; other paths 404/405), `CABLY_BRIDGE` (`BuildAuthUrl` = `<auth page>?port=P&state=S`; `RefreshSession` = `POST {SUPABASE}/auth/v1/token?grant_type=refresh_token` with `apikey` + JSON `{refresh_token}`, a 4xx clears the stored session; `ValidateSession` = `GET {SUPABASE}/auth/v1/user` with `apikey` + `Authorization: Bearer`, one refresh-and-retry on 401; `ListProjects` = `GET {SUPABASE}/rest/v1/projects?select=id,name,updated_at&order=updated_at.desc&limit=50`; `FetchProject` = `GET …/projects?id=eq.<id>&select=data` → `data.project`; `ExportProject` = `POST {ENGINE}/v1/export` with `Authorization: Bearer` + `Content-Type: application/json`, body exactly `{apiVersion:1, project}` → `{kicadPcb, kicadSch}`; `WriteProjectFolder(root, stem, pcb, sch)` writes `<root>/<safe stem>/<stem>.kicad_{pro,pcb,sch}` + sidecar `.cably-export.json` of sha256s and NEVER overwrites a file that differs from both its baseline and the new text, or has no baseline, unless forced — the web app's desktop hand-off rule; the `.kicad_pro` is only ever created). Seams: `CABLY_HTTP` (`cably_bridge_http_kicad.cpp`: `KICAD_CURL_EASY`, redirects off, 15 s connect / 120 s total) and `CABLY_SECRET_STORE` (`cably_bridge_keychain.cpp`: macOS Security.framework generic password, service `dev.cably.desktop`, account `session`, `kSecAttrAccessibleWhenUnlockedThisDeviceOnly`; in-memory store for tests; Windows/Linux stores NOT implemented — every call fails with LastError set). `cably/src/cably_config.h`: + `CABLY_SUPABASE_URL`, `CABLY_SUPABASE_PUBLISHABLE_KEY` (public: it ships in every web bundle), `CABLY_ENGINE_URL`, `CABLY_DESKTOP_AUTH_URL`, `CABLY_APP_URL`, `CABLY_KEYCHAIN_SERVICE`. `cably/CMakeLists.txt` F4 block: sources on `kicad` + `picosha2` + Security/CoreFoundation frameworks; test CLI `cably-bridge-cli` (`cably/src/cably_bridge_cli.cpp`, EXCLUDE_FROM_ALL, never packaged). Tests: `cably/tests/unit/test_cably_bridge.cpp` (229 checks, standalone clang++, fake HTTP) and `cably/tests/bridge.sh` (builds both, then drives the CLI against `cably/tests/mock_cloud.py` playing Supabase + engine on 127.0.0.1: list/open/conflict/--force/expired-bearer-refresh/exact headers, loopback round-trip with curl incl. wrong state, 127.0.0.1-only bind and listener closed after the handoff, keychain round-trip on a throw-away service; 46 checks). Mutants killed by the unit test: state comparison removed, accept-once guard removed, clobber rule inverted, clobber rule over-strict. Not wired into the manager UI yet (F4 home/manager agents); nothing in the fork orchestrates generation. |
| 2026-09-03 | toolchain | Build agent (F4): incremental rebuild of the app after the F4 commits (stamps deleted per cably/toolchain/README.md; only `cably_home_panel.cpp` and the `cably_config.h` dependents recompiled, the bridge objects were already built; no compile errors, 72 s wall). Green on the rebuilt app: bridge.sh 46/46, home.sh (incl. the F4 strings, placeholder gone, 12+47 unit checks, launch), identity.sh, icons.sh, theme.sh with the bundle's own kicad-cli, F1 launch check (open -a, alive at 12 s, exits on SIGTERM). This row also rewords the earlier identity row that quoted the trademark grep verbatim, so the check is empty repo-wide again. |
