# Changes from upstream KiCad (GPLv3 §5(a) record)

Base: KiCad tag 10.0.6 (gitlab.com/kicad/code/kicad).

| date | area | change |
|---|---|---|
| 2026-09-02 | repo | Forked at 10.0.6; added NOTICE.md, CHANGES.md, `cably/` for additions; upstream remote kept for rebasing. No source files modified yet. |
| 2026-09-03 | theme | Added `cably/themes/Cably.json` and `Cably Dark.json` (181 colour keys, schema 5, generated from cably.dev's palette) and `cably/tests/theme.sh` (key completeness against color_settings.cpp + kicad-cli render oracle with negative control). Data only; no source changes. |
| 2026-09-03 | toolchain | `cably/toolchain/`: the kicad-mac-builder patch (ngspice clang flag, 3D-models step no-op, conditional docs copy, tolerant ad-hoc signing) and the exact macOS 26 build recipe. F1 acceptance passed: app launches; Gerbers/netlist match the official 10.0.1 build. No KiCad source changes. |
| 2026-09-03 | icons | Replaced KiCad's application and document icons with Cably's own family (a rounded tile in Cably blue carrying a cable-shaped "C" soldered into two pads; per-editor accent badges; paper+mark document icons). Sources: `cably/icons/make-svgs.py` -> `cably/icons/src/*.svg` (18); `cably/icons/build-icns.sh` renders them with rsvg-convert (qlmanage fallback) and packs with iconutil, overwriting the 18 `*.icns` under kicad/, pcbnew/, eeschema/, gerbview/, pagelayout_editor/, pcb_calculator/, bitmap2component/, cvpcb/, the in-app logo bitmaps `resources/bitmaps_png/png/{icon_kicad*,project_kicad*}.png` (48, used by the About dialog, window icons and the project tree), their `sources/{light,dark}/*.svg`, and `resources/bitmaps_png/icons/icon_kicad{,_64}.png` + `icon_kicad{,_nightly}.ico` (PNG-entry ICO). `cably/tests/icons.sh` proves the tree is byte-identical to a fresh regeneration, that each icns round-trips through iconutil with 16..1024 slices showing the mark, and that every file differs from upstream 10.0.6. No C++/CMake changes; `resources/bitmaps_png/icons/icon_kicad*.xpm` (unused by the build) and `sources/*/kicad_logo*.svg` (unreferenced) still carry KiCad's artwork. |
| 2026-09-03 | home | F3(d): Cably home screen. New `cably/src/cably_home_panel.{h,cpp}` (wxPanel: title, "Describe the circuit you want" prompt with a disabled Generate button — no network until the cloud bridge — recent projects from the manager's FILE_HISTORY, Open project via `KICAD_MANAGER_ACTIONS::openProject`, footer opening About via `ACTIONS::about`) and `cably/src/cably_home_recent.h` (pure helper + `cably/tests/unit/test_cably_home_recent.cpp`). New `cably/CMakeLists.txt` seam (target_sources on `kicad`). MODIFIED `kicad/kicad_manager_frame.{h,cpp}`: the home panel is a second wxAUI centre pane; `updateCablyHomeVisibility()` shows it while no project is loaded and swaps back to the project tree + editors notebook once one is (called from the constructor, LoadProject, CloseProject); tree width only persisted while shown; language change forwarded. Acceptance: `cably/tests/home.sh`. |
| 2026-09-03 | identity | F2 — the product identifies as **Cably Desktop, based on KiCad**. `resources/macos/plist/*.Info.plist.in` (7): CFBundleIdentifier `org.kicad.*` → `org.cably.desktop` / `org.cably.desktop.{pcbnew,eeschema,gerbview,pcb-calculator,pl-editor,bitmap2component}`, CFBundleName + new CFBundleDisplayName "Cably Desktop" / "Cably PCB Editor" / "Cably Schematic Editor" / "Cably Gerber Viewer" / "Cably PCB Calculator" / "Cably Drawing Sheet Editor" / "Cably Image Converter", CFBundleGetInfoString "Cably Desktop, based on KiCad <version>", NSHumanReadableCopyright now names both The KiCad Developers and Cably. `MACOSX_BUNDLE_GUI_IDENTIFIER` aligned in kicad/, pcbnew/, eeschema/, gerbview/, pcb_calculator/, pagelayout_editor/, bitmap2component/ CMakeLists.txt (GPL header added to those seven files). `kicad/CMakeLists.txt`: top-level launcher symlinks renamed to the "Cably …" names above and a `Cably Desktop.app` symlink added; the bundle directory stays `KiCad.app` (kicad-mac-builder addresses it by that path; F6 packaging presents the product name). `kicad/kicad.cpp`: wxApp display name and manager frame title → CABLY_PRODUCT_NAME. `kicad/kicad_manager_frame.cpp`: About title and both window-title sites "KiCad [x.y]" → "Cably Desktop" (no version in the title: the version is KiCad's and is attributed in About). About titles `_HKI( "KiCad <Editor>" )` → `"Cably <Editor>"` in pcbnew (pcb_edit, footprint_edit, footprint_viewer), eeschema (sch_edit, symbol_viewer, symbol_edit), gerbview, pagelayout_editor, pcb_calculator, bitmap2component frames (msgids changed; these 10 strings fall back to English until re-translated). `common/dialog_about/AboutDialog_main.cpp`: description now "Cably Desktop … based on KiCad"; new "Cably Desktop" section with "based on KiCad — Copyright © The KiCad Developers", Source code (CABLY_SOURCE_URL), Cably website, Cably bug link; KiCad website/dev/library links kept as attribution; KiCad bug-tracker and forum links removed (fork issues must not go to KiCad) and a trademark/non-affiliation note added; license page keeps the GPL-3.0-or-later notice and adds the corresponding-source URL and the copyright split. New: `cably/src/cably_config.h` (CABLY_PRODUCT_NAME, CABLY_BASED_ON, CABLY_WEBSITE_URL, CABLY_SOURCE_URL + CABLY_BUGS_URL — PLACEHOLDER URLs, F7 must set the real GPLv3 §6 source location before distribution), top-level `include( cably/CMakeLists.txt )` seam + F2 block in it, `cably/tests/identity.sh` (asserts bundle ids/names on the built app, About strings in the linked binaries, launcher symlinks; optional live-launch check). Not touched: internal identifiers, translation keys other than the 10 About titles, file extensions/type names, kicad-cli, icons, vendor name for config paths. |
| 2026-09-03 | toolchain | Build agent: `cably/toolchain/kicad-mac-builder-macos26.patch` was never in git (upstream `.gitignore` ignores `*.patch`; force-added) and the live `kicad-mac-builder/kicad.cmake` it captured was corrupted after the F1 build (the first 122 lines pasted a second time inside the `if(DEFINED REDISTRIBUTABLE)` block, leaving it unclosed, and the sign-app step was NOT tolerant despite the README). Regenerated from upstream + the three intended hunks: conditional docs copy, packages3d no-op, and sign-app wrapped in `sh -c "... || echo"` so the macOS 26 ad-hoc codesign failure on the embedded Python.framework no longer aborts the build. README: how to force an incremental rebuild (ExternalProject stamps). No KiCad source changes. |
| 2026-09-03 | home | Build agent: `cably/tests/home.sh` check (a) gave false FAILs (`set -o pipefail` + `grep -q` exiting early -> `strings` SIGPIPE -> non-zero pipeline status); now `grep -F >/dev/null`. First green run on the rebuilt app. |
| 2026-09-03 | identity | Reworded a comment in cably/src/cably_config.h so the trademark check (a `git grep` for the product name followed by the KiCad mark must be empty) holds. |
| 2026-09-03 | home | F4 (agent home-f4): the home panel talks to the cloud bridge. `cably/src/cably_home_panel.{h,cpp}` rewritten around two visible paths: (1) the prompt's **Generate** now opens `https://cably.dev/app?prompt=<percent-encoded>` in the system browser (tooltip "Generates on cably.dev; open the result here afterwards"; generation is NOT orchestrated by the fork); (2) a "Your Cably projects" card: **Sign in to Cably** starts the bridge's 127.0.0.1 loopback, opens the hand-off URL, shows "Waiting for the browser…" with Cancel; on success "Signed in as <email>", the session goes to the keychain through the bridge and the user's projects are listed newest first (name + "updated 5 min ago"/date) with an **Open** button each: FetchProject → ExportProject (engine.cably.dev) → WriteProjectFolder under `~/Documents/Cably Desktop/<stem>/` → `KICAD_MANAGER_FRAME::LoadProject`. A conflict (files KiCad edited since the last export) asks "This project was edited here since Cably last exported it." with Keep / Replace, mirroring the web app's desktop button. Sign out clears the keychain entry via the bridge; a stored session is validated at start-up and dropped with a reason if dead. Every bridge call runs on a detached thread, results return via CallAfter behind a liveness token + serial (cancel/sign-out discard stale results). The bridge (`cably/src/cably_bridge.h`, one `CABLY_BRIDGE` over `CABLY_HTTP_KICAD` + `CABLY_KEYCHAIN_SECRET_STORE(CABLY_KEYCHAIN_SERVICE)`, single-threaded by design) is owned by the panel behind a mutex and reached ONLY through the adapter section at the top of cably_home_panel.cpp; the sign-in wait is sliced (`CABLY_LOOPBACK_SERVER::WaitForSession` 500 ms at a time, 10 min cap) so Cancel is a flag and the waiting worker stops the listener itself — no loopback is destroyed under a thread blocked on it; success calls `FinishLoopback(0)` (persists + stops). `WriteProjectFolder` is the bridge's static one (root = ~/Documents/Cably Desktop, it applies SafeStem and the .cably-export.json rule); a 401-cleared session (`!HasSession()`) drops the UI to signed-out with the reason. Verified: the panel TU passes `-fsyntax-only` with the build's own flags against the landed bridge header. New `cably/src/cably_home_cloud.h` (pure: CablyProjectStem mirrors the web app's safeProjectFileStem; CablyParseIsoUtc; CablyFormatUpdated; CablyPercentEncode; CablyGenerateUrl) + `cably/tests/unit/test_cably_home_cloud.cpp` (47 checks, green). `cably/tests/home.sh`: asserts the new strings in the binary, that the F3 placeholder "coming in the next phase" is gone, and builds/runs both unit tests. `cably/CMakeLists.txt`: F4 block. Only the public website URL (CABLY_WEBSITE_URL) is named here; Supabase/engine endpoints live in the bridge. |
| 2026-09-03 | bridge | F4: cloud bridge (C++, no GUI yet). New `cably/src/cably_bridge.{h,cpp}`: pure C++17 core (nlohmann/json + picosha2 headers, POSIX sockets) — `CABLY_LOOPBACK_SERVER` (sign-in handoff: binds 127.0.0.1:<kernel port>, 32-byte hex `state`; `GET /callback` serves a page whose script reads `location.hash` and POSTs it as JSON to `/token` on the same origin; `POST /token` accepted only when `state` matches, exactly once, then the listener closes; other paths 404/405), `CABLY_BRIDGE` (`BuildAuthUrl` = `<auth page>?port=P&state=S`; `RefreshSession` = `POST {SUPABASE}/auth/v1/token?grant_type=refresh_token` with `apikey` + JSON `{refresh_token}`, a 4xx clears the stored session; `ValidateSession` = `GET {SUPABASE}/auth/v1/user` with `apikey` + `Authorization: Bearer`, one refresh-and-retry on 401; `ListProjects` = `GET {SUPABASE}/rest/v1/projects?select=id,name,updated_at&order=updated_at.desc&limit=50`; `FetchProject` = `GET …/projects?id=eq.<id>&select=data` → `data.project`; `ExportProject` = `POST {ENGINE}/v1/export` with `Authorization: Bearer` + `Content-Type: application/json`, body exactly `{apiVersion:1, project}` → `{kicadPcb, kicadSch}`; `WriteProjectFolder(root, stem, pcb, sch)` writes `<root>/<safe stem>/<stem>.kicad_{pro,pcb,sch}` + sidecar `.cably-export.json` of sha256s and NEVER overwrites a file that differs from both its baseline and the new text, or has no baseline, unless forced — the web app's desktop hand-off rule; the `.kicad_pro` is only ever created). Seams: `CABLY_HTTP` (`cably_bridge_http_kicad.cpp`: `KICAD_CURL_EASY`, redirects off, 15 s connect / 120 s total) and `CABLY_SECRET_STORE` (`cably_bridge_keychain.cpp`: macOS Security.framework generic password, service `dev.cably.desktop`, account `session`, `kSecAttrAccessibleWhenUnlockedThisDeviceOnly`; in-memory store for tests; Windows/Linux stores NOT implemented — every call fails with LastError set). `cably/src/cably_config.h`: + `CABLY_SUPABASE_URL`, `CABLY_SUPABASE_PUBLISHABLE_KEY` (public: it ships in every web bundle), `CABLY_ENGINE_URL`, `CABLY_DESKTOP_AUTH_URL`, `CABLY_APP_URL`, `CABLY_KEYCHAIN_SERVICE`. `cably/CMakeLists.txt` F4 block: sources on `kicad` + `picosha2` + Security/CoreFoundation frameworks; test CLI `cably-bridge-cli` (`cably/src/cably_bridge_cli.cpp`, EXCLUDE_FROM_ALL, never packaged). Tests: `cably/tests/unit/test_cably_bridge.cpp` (229 checks, standalone clang++, fake HTTP) and `cably/tests/bridge.sh` (builds both, then drives the CLI against `cably/tests/mock_cloud.py` playing Supabase + engine on 127.0.0.1: list/open/conflict/--force/expired-bearer-refresh/exact headers, loopback round-trip with curl incl. wrong state, 127.0.0.1-only bind and listener closed after the handoff, keychain round-trip on a throw-away service; 46 checks). Mutants killed by the unit test: state comparison removed, accept-once guard removed, clobber rule inverted, clobber rule over-strict. Not wired into the manager UI yet (F4 home/manager agents); nothing in the fork orchestrates generation. |
| 2026-09-03 | toolchain | Build agent (F4): incremental rebuild of the app after the F4 commits (stamps deleted per cably/toolchain/README.md; only `cably_home_panel.cpp` and the `cably_config.h` dependents recompiled, the bridge objects were already built; no compile errors, 72 s wall). Green on the rebuilt app: bridge.sh 46/46, home.sh (incl. the F4 strings, placeholder gone, 12+47 unit checks, launch), identity.sh, icons.sh, theme.sh with the bundle's own kicad-cli, F1 launch check (open -a, alive at 12 s, exits on SIGTERM). This row also rewords the earlier identity row that quoted the trademark grep verbatim, so the check is empty repo-wide again. |
| 2026-09-03 | bridge | Test CLI prints token LENGTHS only (never values); GPL headers on the shell tests; check-gpl-headers.sh now inspects *.sh. |
| 2026-09-03 | home | F5: sync UI. `cably/src/cably_home_panel.{h,cpp}`: while a project whose folder carries the bridge's `.cably-export.json` sidecar is loaded in the manager (detected from the panel's own wxEVT_SHOW — a load hides the pane, a close shows it — plus a 1.5 s safety-net timer, and the public `NotifyProjectState()` the frame may call; no manager-frame edit), its folder is watched and each KiCad save is imported into the cably.dev project through the bridge (`ImportProject`, off the UI thread under the bridge mutex; a save that lands mid-import runs once more afterwards). Outcomes: a status line on the home panel ("Synced to Cably · 12:04 · 2 nets renamed", Details button opening the full report in a dialog) and one non-modal manager notification (key `cably-sync`; title = that line, description = "Dropped: <first> (+N more)" or the headline, "View Details" → the full report written to `<folder>/.cably-sync-report.txt`); `cloud-changed` → dialog "This project changed on cably.dev since it was opened here." with "Keep my KiCad edits (overwrite cloud)" (forces the PATCH) / "Discard my edits (reload from Cably)" (closes the project, re-runs the F4 fetch→export→WriteProjectFolder with force, loads it; the cloud project is the one the folder was opened from this session, else the listed one whose stem matches the folder) / Cancel; error → reason + Retry (also on the next save). A folder without the sidecar is never synced. The F4 open path is factored into `cloudFetchExportWrite` (shared with reload), now passes the `CABLY_EXPORT_META` (projectId, projectName, the row's updated_at from `FetchProject`, engineVersion) to `WriteProjectFolder` so the sidecar names its cloud row, and remembers folder → cloud project. Sync-adapter section at the top of the .cpp is the only place naming the sync-bridge's types (`cably/src/cably_sync.h`, agent "sync-bridge": `CABLY_PROJECT_WATCH::Start(dir, callback, options, &error)` — callback on the watcher thread, marshalled by CallAfter under the liveness lock — and `CablySyncSave(bridge, dir, event)` under the bridge mutex; a save that lands mid-import is queued newest-per-path and sent afterwards; `NOT_CABLY_PROJECT` stops the watch and says to open the project from the list again). "Keep my KiCad edits (overwrite cloud)" = blank the sidecar's `cloudUpdatedAt` (`CABLY_EXPORT_SIDECAR::Load/Save`) so `ImportProject` skips its newer-than check, then `CablySyncSave` again (which records the new version). New `cably/src/cably_sync_status.h` (pure: `CablySummariseImport` over the engine's pcbReport/schReport JSON → counts, headline with singular/plural wording, grouped full text; `CablySyncDroppedDigest`; `CablySyncClock`; `CablySyncStatusLine`; `CablySidecarPath`; `CablySyncReportPath`) + `cably/tests/unit/test_cably_sync_status.cpp` (47 checks, green; red first: header missing). `cably/tests/home.sh`: asserts the new strings in the binary ("Synced to Cably", "changed on cably.dev since it was opened here", both dialog answers, "Couldn't sync to Cably", "Retry"), builds/runs the third unit test with the nlohmann include, launch check kept. `cably/CMakeLists.txt`: F5 sync-home block. Not built here (rebuild is the build agent's); the panel TU was checked with `-fsyntax-only` and the build's own flags. |
| 2026-09-03 | sync | F5 (fork half): a KiCad save goes back to the cloud project. New `cably/src/cably_sync.{h,cpp}`: `CABLY_PROJECT_WATCH` watches ONE folder opened from Cably (identified by the `.cably-export.json` sidecar) and mirrors the Electron companion's watcher (desktop/src/main/watcher.ts) rule for rule — classify (`.kicad_pcb` → board, `.kicad_sch` → schematic; `-bak`, `.lck`, `~`, `_autosave-`, `_saved_`, `*-backups/` ignored), per-path debounce (default 500 ms), content-hash dedupe with the baseline seeded from the sidecar's hashes (disk for documents it does not list) and updated on every accepted save, `Expect(path, text)` so Cably's own hand-off write is not a save; emits `CABLY_SAVE_EVENT{kind, path, text}` on the watcher thread. Mechanism: POLLING on its own thread (default 250 ms; stat inode/size/mtime-ns of the folder's documents), wx-free like the rest of the bridge so `cably/tests/bridge.sh` builds it standalone. `wxFileSystemWatcher` was measured and rejected: on macOS wx 3.2 backs `Add()` with kqueue, which asserts unless a wx loop already runs, and delivered 0 events for an in-place write and a rename-over in a console-loop harness in both `Add` and `AddTree` (FSEvents) modes. Measured with the real watcher against the mock cloud (bridge.sh prints it): save → synced in ~0.38–0.45 s with debounce 300 / poll 50 incl. the four HTTP calls; unit-level in-place latency ~105–120 ms with debounce 100 / poll 20. `CablySyncSave(bridge, dir, event)`: sidecar in → `ImportProject` → sidecar out (new `cloudUpdatedAt`, `files[<name>]` = sha256 of the saved text, `engineVersion`); `NOT_CABLY_PROJECT` when the sidecar is missing or has no projectId. MODIFIED `cably/src/cably_bridge.{h,cpp}`: sidecar now also records `projectId`, `projectName`, `cloudUpdatedAt` (the row's `updated_at` at export) and `engineVersion` (typed `CABLY_EXPORT_SIDECAR` Load/Save; `WriteProjectFolder(..., const CABLY_EXPORT_META* = nullptr)` keeps the previous meta when none is given; clobber rule unchanged, its tests green); `FetchProject(id, json, std::string* updatedAt = nullptr)` selects `data,updated_at` when asked; `Sha256Hex`, `CompareIsoTimestamps` (microsecond ISO-8601 with `Z`/`±HH:MM`); `ImportProject(id, kicadPcb?, kicadSch?, expectedUpdatedAt, out)` = `GET …/projects?id=eq.<id>&select=data,updated_at` (apikey + bearer) → CLOUD-CHANGED RULE: if the row's `updated_at` is newer than `expectedUpdatedAt` (or either is unparsable and they differ) return `CLOUD_CHANGED` and touch nothing → `POST {engine}/v1/import` (bearer only) body exactly `{apiVersion:1, project: data.project[, kicadPcb][, kicadSch]}` → `PATCH …/projects?id=eq.<id>` (apikey + bearer, `Content-Type: application/json`, `Prefer: return=minimal`) body `{"data": <fetched data with ONLY .project replaced — schema and chat kept>}` → `GET …/projects?id=eq.<id>&select=updated_at` for the trigger-stamped version (the web app relies on the `update_projects_updated_at` trigger; `return=minimal` carries none). The token goes to those two hosts only. `cably/src/cably_bridge_http_kicad.cpp`: PATCH support (`CURLOPT_CUSTOMREQUEST`). `cably/src/cably_bridge_cli.cpp`: `open` records the meta; new `import <id>` and `watch <dir>` commands (`--once`, `--debounce`, `--poll`, `--watch-timeout`, `--self-write`). `cably/CMakeLists.txt`: F5 block. Tests (written first, red then green): `cably/tests/unit/test_cably_sync.cpp` (206 checks: the Electron classification table, in-place save with latency, burst → one event with the final text, atomic rename, backups/locks/autosaves silent, own write via Expect silent then the next real save heard, identical rewrite silent, nothing after Stop, baseline from the sidecar, sidecar round trip, timestamp ordering, ImportProject's exact four requests with chat byte-equal, kicadPcb/kicadSch/both/neither, cloud-changed refuses to PATCH, equal instants in `Z`/`+00:00` notation, older row proceeds, engine/PATCH failures, legacy row, 401 → refresh once, CablySyncSave sidecar in/out); `test_cably_bridge.cpp` 229 → 252 (sidecar meta, FetchProject version). `cably/tests/mock_cloud.py`: `/v1/import`, `PATCH /rest/v1/projects` (return=minimal enforced, body must be exactly `{data}`, trigger-like `updated_at` bump), `select=data,updated_at|updated_at`, test-control `POST /__mock/touch` and `GET /__mock/row`. `cably/tests/bridge.sh` (79 checks, PASS): + sync unit test, four mutants that must FAIL the unit test (content dedupe removed → our own write becomes a save; `updated_at` comparison inverted; cloud-changed check removed; debounce ignored — all killed), and against the mock with the REAL watcher: in-place save → exactly one `POST /v1/import` + one `PATCH` with the exact headers/bodies, write-temp-then-rename save (schematic) likewise, `-bak`/`.lck`/`_autosave-`/`_saved_`/`*-backups/` writes → nothing, `--self-write` (Expect) → nothing then the next real save heard, cloud touched → `cloud-changed`, one GET, NO PATCH, sidecar and row untouched. Not wired into the manager UI: the home panel's `WriteProjectFolder` call still passes no meta (one-line change: fetch with `&updatedAt`, pass a `CABLY_EXPORT_META`) and nothing starts a watch yet (F5 UI agent). App not rebuilt by this change. |
| 2026-09-03 | toolchain | Build agent (F5): two incremental rebuilds of the app after the F5 commits (stamps deleted per cably/toolchain/README.md, `--jobs 4`): #1 recompiled 7 TUs (`cably_home_panel`, `cably_bridge`, `cably_bridge_keychain`, `cably_bridge_http_kicad`, `cably_sync`, `kicad_manager_frame`, `build_version`) with no compile errors, 103 s wall; `cably/tests/home.sh` then failed exactly one assertion — "Synced to Cably" — because that string was the only F5 UI string spelled as a `wxS()` literal (`cably/src/cably_sync_status.h`, `CablySyncStatusLine`), which on macOS is a wide UTF-32 literal that `strings` cannot see, while the panel's sibling strings are narrow `_()` literals. Fix: `wxString::FromUTF8( "Synced to Cably" )` (same text, same unit-test result 47/47); rebuild #2 recompiled 1 TU, 81 s wall. Green on the rebuilt app: bridge.sh 79/79 (252 + 206 unit checks, 4 mutants killed, real-watcher save→synced 391 ms), home.sh (all 15 strings, placeholder gone, 12+47+47 unit checks, launch), identity.sh, icons.sh, theme.sh with the bundle's own kicad-cli; explicit launch check `open -a <bundle>` now resolves to OUR bundle (org.cably.desktop), alive at 12 s, exits on SIGTERM. check-gpl-headers.sh OK; trademark grep empty. |
| 2026-09-03 | toolchain | Unsigned dev DMG via `package-kicad-unified` with the 3D-model/docs dependencies trimmed (patch + README); verified by mount + launch. |
| 2026-09-03 | toolchain | F6 (agent dmg-identity): the DMG presents **Cably Desktop**. `cably/tests/dmg.sh` (written first; RED on the F6-lite image: name `kicad-unified-…`, volume "KiCad", folder "KiCad", KiCad's `background.png` riding along, KiCad's volume icon, .DS_Store naming KiCad) mounts a DMG read-only at a private mountpoint and asserts: file name `^cably-desktop-.*\.dmg$`, volume name, top level = {`Cably Desktop/`, `Applications -> /Applications`, `demos/`} with nothing named KiCad (hidden entries included), `Cably Desktop/Cably Desktop.app` -> bundle id org.cably.desktop, `.background.png` byte-equal to a fresh rsvg-convert render of the new `cably/icons/src/dmg-background.svg` (660x400: the Cably mark, "Drag Cably Desktop to Applications", an arrow, a GPL footer; no upstream artwork) and not byte-equal to either of the toolchain's backgrounds, `.VolumeIcon.icns` == `kicad/kicad.icns` + custom-icon flag on the root, `.DS_Store` names our items and carries an icvp background alias to `.background.png`, launch FROM THE MOUNT via `open -a` (live executable path under the mountpoint), alive at 12 s, exits on SIGTERM. Template: `cably/toolchain/dmg/make-template.sh` creates `cablytemplate.dmg.tar.bz2` (158,573 bytes committed; a 200 MB HFS+ GPT UDRW image package.sh grows with `hdiutil resize`) with no Finder: `hdiutil create`, `mount -t hfs`, the symlink/folders, the rendered background, the icns, `SetFile -a C`, and a `.DS_Store` from the new `cably/toolchain/dmg/make-ds-store.py` (pure python: the Buddy-allocator/B-tree layout of ds_store + an Alias-v2 record like mac_alias, i.e. what dmgbuild writes; `dump` mode reads any .DS_Store — validated on KiCad's own template file — and the alias was verified to resolve to `/Volumes/Cably Desktop/.background.png` through `CFURLCreateBookmarkDataFromAliasRecord` + `URL(resolvingBookmarkData:)` while the template was mounted there; window {{100,100},{660,400}}, icons at (165,170)/(495,170)/(330,300), icon size 96). Toolchain patch (`kicad-mac-builder-macos26.patch`, regenerated; README documents it): `package_kicad_unified.cmake` cache var `CABLY_DMG_TEMPLATE` defaulting to `<kicad source dir>/cably/toolchain/dmg/cablytemplate.dmg.tar.bz2`, passed to `bin/package.sh`, which untars it instead of KiCad's, skips the `PACKAGING_DIR/background.png` copy, sets `MOUNT_NAME='Cably Desktop'` and renames the mounted template volume to it (`diskutil rename`, in place — MOUNT_NAME alone did not drive the volume name; reverting it now makes dmg.sh FAIL on "volume name = 'KiCad'", checked), fills a `Cably Desktop/` folder (demos still moved next to it) and names the output `cably-desktop-<KICAD_SEMANTIC_VERSION>-<date>-<rev>.dmg` (version from the built app's CFBundleShortVersionString, `git describe --abbrev=0` fallback). Built: `cably-desktop-10.0.6-20260903-083045-d7e3c3d0.dmg`, 274,746,685 bytes (272 MB), dmg.sh PASS (26 checks incl. launch from the mount). Still KiCad-named inside the image: the `KiCad.app` bundle directory under `Cably Desktop/` (toolchain path; its identity is org.cably.desktop / "Cably Desktop"), `kicad-cli`, and the About/NOTICE attribution. Not done: Developer ID signing/notarization, Finder-visual check of the layout (no GUI in this session; the alias resolution above is the programmatic proof). |
| 2026-09-03 | repo | Published at https://github.com/NevoMir/cably-desktop (fork of KiCad/kicad-source-mirror, default branch cably-main). CABLY_SOURCE_URL/CABLY_BUGS_URL set to the real repository; README gains a Cably section above KiCad's. |

| 2026-09-03 | linux | Agent linux-ci (two instances of this job ran in the shared checkout and both committed "Linux: GitHub Actions build" — 75ac5b6e75 then 899d7c741a; this row and the tree after the reconciling commit are the record): `.github/workflows/linux.yml` builds Linux x86_64 on GitHub Actions (ubuntu-24.04 runner; push to cably-main, tags `v*`, pull_request, workflow_dispatch; `concurrency` cancels in-progress runs of the same ref; `timeout-minutes: 330`; checkout `fetch-depth: 0` so KiCad's `git describe` versions the build, no submodules; no secrets beyond GITHUB_TOKEN). It sequences the shared Linux scripts by name and duplicates none of their logic: apt from the new `cably/linux/apt-packages.txt` (the Dockerfile.build package set factored into one-per-line form, 58 packages incl. the port agent's libspnav/poppler-glib/desktop-file-utils additions; `cably/tests/linux-ci.sh` fails when the two sets drift; ccache is CI-only), a setup step writing the `CABLY_*` knobs to the job env (build tree on the runner's /mnt scratch disk, /opt/cably-desktop chowned, `CABLY_JOBS=$(nproc)`, `CABLY_DEB_DIR`, `CABLY_FIXTURES` = `cably/tests/fixtures/` when the checkout has blinking-led.* else copies of `demos/ecc83`, ccache as compiler launcher through `CABLY_CMAKE_EXTRA`), `cably/linux/build.sh` (timings to the step summary), `cably/linux/run-tests.sh` under `xvfb-run`, `cably/linux/package-deb.sh` run inside `CABLY_DEB_DIR` (the .deb checked with `dpkg-deb --info/--field Package ~ cably`), `actions/upload-artifact` `cably-desktop-linux-x86_64-deb` (`if-no-files-found: error`); on a `v*` tag a separate `release` job (the only one with `contents: write`) attaches it via softprops/action-gh-release@v2 with the body "Linux x86_64 .deb for Ubuntu 24.04; based on KiCad; GPL-3.0-or-later; source = this repository". ccache: `actions/cache/restore` keyed on the gcc version + `hashFiles` of every CMakeLists.txt, `cmake/**` and `cably/**/*.cmake` with a sha suffix (restore-keys fall back to the newest cache of the same hash, then compiler), saved by an explicit `actions/cache/save` with `if: always()` so a failed build or test still keeps its objects. A fast `lint` job runs check-gpl-headers.sh, the trademark grep (phrase assembled at runtime so no file carries it) and linux-ci.sh with actionlint. New `cably/tests/linux-ci.sh` (written first, RED: workflow and package list missing; then GREEN, 55 checks): GPL notice, YAML parse, actionlint clean (shellcheck on every run block), every requirement above asserted on the text, no secrets beyond GITHUB_TOKEN, trademark grep, apt-packages.txt == Dockerfile.build's apt-get set. Validated locally with actionlint 1.7.12 and a ruby YAML parse only — no Docker/compile was run here (the port agent owns the only build). Not verified: the real run (expected cold 2-3 h on a 4-vCPU runner, warm ccache tens of minutes); package-deb.sh's interface is assumed (CABLY_FORK/CABLY_BUILD_DIR/CABLY_PREFIX in, .deb into `$CABLY_DEB_DIR` = cwd); a libsecret-only Linux secret store would fail run-tests.sh on a headless runner (no Secret Service) unless it has a fallback. Not pushed here: the orchestrator pushes and watches the first run. |
| 2026-09-04 | linux | Agent linux-port, build tooling: `cably/linux/Dockerfile.build` (Ubuntu 24.04 build+test image; its apt list == `cably/linux/apt-packages.txt`, `cably/tests/linux-ci.sh` fails on drift), `cably/linux/build.sh` (configure `-DKICAD_USE_CMAKE_FINDPROTOBUF=ON -DKICAD_SCRIPTING_WXPYTHON=OFF -DKICAD_BUILD_QA_TESTS=OFF -DKICAD_BUILD_I18N=OFF`, RelWithDebInfo, prefix `/opt/cably-desktop`, `CMAKE_INSTALL_RPATH` = `$ORIGIN/../lib;$ORIGIN/../../..;$ORIGIN/../../../lib` so the installed tree runs without `LD_LIBRARY_PATH` — without it the installed `kicad-cli` could not load `libkicommon.so.10.0.6`; install into an emptied prefix, guarded by a marker file so only a prefix this script populated is wiped; per-step timings and the installed `kicad-cli version`), `cably/linux/run-tests.sh` (unit tests with the system clang++ and the distro wx, `bridge.sh` against the Ninja tree, `theme.sh` with the installed kicad-cli and `cably/tests/fixtures/blinking-led.{kicad_pcb,kicad_sch}` — the D1 fixtures copied from Cably-pcb/desktop/test-fixtures so CI has them — launch under Xvfb, `identity-linux.sh`, `package-deb.sh` + `deb.sh`), `cably/linux/README.md`. Measured: first full compile in Docker on an M-series Mac (arm64, `--cpus 8 --memory 28g`, `-j6`) wrote its 2207 objects between 21:53 and 12:17 UTC over a night in which the host slept part of the time (the container ran with `--rm`, so build.sh's own step timings are gone with it); the incremental rebuild after the identity changes took 43 s (2 s configure, 30 s ninja, 5 s install), a no-op rebuild 13 s. |
| 2026-09-04 | linux | Linux secret store (a signed-in Cably session survives a restart on Linux). `cably/src/cably_bridge_keychain.cpp`, non-Apple half: the freedesktop Secret Service through libsecret (schema `dev.cably.desktop`, attributes service/account, default collection) when a session bus exists (`DBUS_SESSION_BUS_ADDRESS` or `$XDG_RUNTIME_DIR/bus` — with neither, GDBus would try to autolaunch a bus and hang under Xvfb) and `secret_service_get_sync` answers; otherwise the documented fallback file `$XDG_CONFIG_HOME/cably-desktop/session.json` (`session.<service>.json` for any other service name; `~/.config` when `XDG_CONFIG_HOME` is unset or relative), mode 0600 in a 0700 directory, written temp-then-rename. A session left in the file is still found and moved into the service by the next Save (the file is removed); a REACHABLE service that fails is an error, never a silent plain-text fallback. `cably/src/cably_bridge.h`: the `CABLY_SECRET_SERVICE` seam (test injects a fake keyring), `Backend()` ("keychain" / "secret-service" / "file:<path>"), `DefaultFallbackDir()`, `FallbackPath()`. `cably/CMakeLists.txt`: libsecret-1 via pkg-config into `kicad` and `cably-bridge-cli` with `CABLY_HAVE_LIBSECRET=1` (KiCad's kiplatform already needs libsecret on Linux, so no new dependency). `cably/src/cably_bridge_cli.cpp` prints `store_backend=` on save/show/signout. Tests: `cably/tests/unit/test_cably_secret_store.cpp` (125 checks: file fallback with 0600/0700 and no temp file left, default paths, corrupt file, the fake service, unavailable service = the file case, migration file → service, store/lookup/clear failures surfaced, the real libsecret backend probed once — in the container it reports unavailable and the file is used; RED first: the earlier draft called `Describe()`, `UsedSecretService()`, a no-argument `FallbackPath()` and `DefaultConfigDir()` that the header never declared — 23 compile errors — and was reconciled to the header's API); `cably/tests/bridge.sh` (e) now runs on Linux too: save → show → signout through the CLI under a scratch `XDG_CONFIG_HOME`, asserting `store_backend=file:<path>`, 0600/0700, and no file left after signout. |
| 2026-09-04 | linux | Linux identity (the F2 rule on the installed tree): `cably/linux/CablyLinuxNames.cmake` sets the product's desktop ids (`KICAD_APP_NAME`/`KICAD_APP_PREFIX` = `org.cably.desktop`, `KICAD_ICON_PREFIX`/`KICAD_DESKTOP_FILE_ICON_*` = `org.cably.desktop{,.}`, `KICAD_PROVIDES_APP_ID` empty; the MIME prefix stays `kicad` — the file formats are KiCad's and a distro KiCad next to us must not fight over them) and is included by a one-line seam at the end of `cmake/KiCadAppNames.cmake` (the fork's GPL header added to that upstream file), so both consumers of those variables see it: the Wayland app_id definition `common/CMakeLists.txt` puts on `pgm_base.cpp`, and the `resources/CMakeLists.txt` rules that name the launchers (`org.cably.desktop.desktop`, `org.cably.desktop.{pcbnew,eeschema,gerbview,pcbcalculator,bitmap2component}.desktop`) and the metainfo (`org.cably.desktop.metainfo.xml`). Text edited in place — the smallest diff, so an upstream merge conflicts visibly on those lines instead of a Cably copy drifting silently: `resources/linux/launchers/*.desktop.in` `Name=` → "Cably Desktop…" (+ the three `Comment=` lines that named KiCad as the product), `resources/linux/metainfo/org.kicad.kicad.metainfo.xml.in` `<name>`, `<description>` ("…is based on KiCad…", the attribution), `<url>`s, `update_contact`, `developer_name`, keyword Cably, and KiCad's own screenshots removed (they are KiCad's images). Icons: `cably/linux/make-icons.sh` renders `cably/icons/src/*.svg` with rsvg-convert into `cably/linux/icons/hicolor` (16/24/32/48/64/128/256 px + scalable; apps `org.cably.desktop*`, mimetypes `application-x-kicad-*` from the `*_doc.svg` sources; 96 files committed), installed by `cably/CMakeLists.txt`; KiCad's own app icons are no longer installed (`KICAD_ICON_PREFIX` matches none of `resources/linux/icons`). `cably/linux/build.sh` also deletes stale `org.kicad.*` outputs from an incremental Ninja tree (nothing regenerates or removes them and the install rule copies the whole directory). Test: `cably/tests/identity-linux.sh` on `/opt/cably-desktop` (72 checks: launcher names/Name=/Icon=/Exec=/MimeType=, desktop-file-validate, Cably icons at every size and byte-different from KiCad's, metainfo id/name/launchable/description/mediatypes/no KiCad screenshot, MIME files kept, `strings bin/kicad` carries "Cably Desktop" and "based on KiCad", kicad-cli runs from the tree, `$ORIGIN` RUNPATH; RED first on the unmodified install: 14 FAIL). |
| 2026-09-04 | linux | Linux packaging: `cably/linux/package-deb.sh` writes `cably-desktop_10.0.6+cably.<yyyymmdd>.<git short rev>_<arch>.deb` into `$CABLY_DEB_DIR` (default cwd): a stripped `cmake --install --strip` under DESTDIR at `/opt/cably-desktop` (3.7 GB RelWithDebInfo tree → 487 MB), launchers copied to `/usr/share/applications` with an absolute `Exec=` + `TryExec=` (the /opt tree is not in `XDG_DATA_DIRS`), the Cably app icons and the metainfo linked into `/usr/share`, the MIME definitions linked as `cably-desktop-{kicad,gerbers}.xml` (no path a distro `kicad` package owns: side-by-side install works), `/usr/bin/cably-desktop` and `cably-desktop-cli`, `/usr/share/doc/cably-desktop/{copyright (DEP-5: GPL-3.0-or-later, `/usr/share/common-licenses/GPL-3`, NOTICE.md, the source URL, KiCad's trademark), NOTICE.md, CHANGES.md.gz}`. Control: Maintainer `Cably <dev@cably.dev>`, Description "Cably Desktop, based on KiCad" (+ GPL notice and source URL), Section electronics, Homepage; `Depends` derived from the binaries with `dpkg-shlibdeps` over every ELF file including the `*.kiface` modules (a `shlibs.local` covers the package's own libkicommon & co.), plus `libsecret-1-0`, `python3` and `libngspice0` (dlopen'd by the simulator) — 35 packages on Ubuntu 24.04 (`t64` names); Recommends desktop-file-utils, hicolor-icon-theme, shared-mime-info, xdg-utils (cache refreshes via dpkg triggers, no maintainer scripts). xz, 107 MB on arm64, ~30 s. Test: `cably/tests/deb.sh` (RED first: no .deb → FAIL): static `dpkg-deb -I/-c` assertions, then — from a Docker host, or on CI — `docker run --rm ubuntu:24.04` with `apt-get update; apt-get install -y ./x.deb xvfb` (apt resolves the Depends: 298 packages in 38 s), `kicad-cli version` = 10.0.6, `cably-desktop` on PATH, launcher validation, `/opt/cably-desktop/bin/kicad` alive 12 s under `xvfb-run` and gone on SIGTERM, `theme.sh` with `CABLY_KICAD_CLI` at the installed kicad-cli. Inside the build container (no docker, not root) the install phase is a WARN with the host command. The fresh install caught two bugs before anything shipped: the `*.kiface` modules (installed 0644, not named `*.so`) were skipped by the ELF scan, so `libpoppler-glib8t64` and `libodbc2` were missing and `kicad-cli … export svg` failed on a clean system; and `grep -q` on a piped `dpkg-deb --contents` listing under `pipefail` reported early entries as missing (SIGPIPE, 141). |
| 2026-09-04 | linux | .deb version made monotonic: `10.0.6+cably.<yyyymmdd>.<N>.g<rev>[.dirty]`, N = commits since the 10.0.6 tag, placed before the rev because dpkg compares a hex rev as text, so a later build of the same day could sort as a downgrade (`dpkg --compare-versions 10.0.6+cably.20260904.1abc0000 lt 10.0.6+cably.20260904.77d2f34d53` was true; found by the independent verifier). N grows with every commit, the day orders first across days, and a rebuild of the same commit keeps its version (a time of day would let a later build of an older commit win); `.dirty` sorts after the clean build and is never a release. `cably/tests/deb.sh` (static section) asserts the format and, with dpkg, that a synthetic later build of the day (N+1, rev 0000000000) sorts greater, the previous (N-1, rev ffffffffff) lower and a later day greater; red first against the .deb the old script wrote. Scheme documented in `cably/linux/package-deb.sh` and `cably/linux/README.md`. |
| 2026-09-04 | linux | The .deb's rev comes from the BUILD TREE, not the checkout: `cably/linux/package-deb.sh` reads `KICAD_VERSION` from `$CABLY_BUILD_DIR/kicad_build_version.h` (falling back to `kicad_build_version.txt`) - KiCad's `git describe --dirty` at the last build, the string the binaries print - for N, rev and the `.dirty` marker; `git describe` in the source is only a fallback, with a warning, for a tree without the record, and a checkout whose `KiCadVersion.cmake` names another tag than the build's is warned about. Before, `git rev-parse --short HEAD` in the checkout named the package after whatever the checkout was at packaging time (the verifier's stale .deb said `20260904.5dd0cfc6e4` while its binaries reported `10.0.6-22-g5dd0cfc6e4-dirty`: wrong dirty state, and a name that would have been wrong for any later HEAD). `deb.sh` now runs the packaged `kicad-cli version --format about` - from the extracted tree where its libraries load (the build container) and always after the install on the fresh Ubuntu - and requires its `<N>-g<rev>[-dirty]` to equal the package Version's; red first against that stale .deb. |
| 2026-09-04 | repo | Secret scan in `cably/scripts/check-gpl-headers.sh` widened (found by the independent verifier: it matched only the service-role env var name, Stripe keys and RSA/OpenSSH key blocks): now also Supabase secret keys (the sb_secret prefix), a service-role JWT (the role claim as it sits base64-encoded in the payload, at each of its three alignments, plus the bare role name; nothing is decoded), EC, DSA and PKCS#8 unencrypted private-key PEM blocks, and AWS access key ids (AKIA + 16 upper-case/digits, not inside a longer base64 run). The publishable anon key in `cably/src/cably_config.h` (a JWT with role=anon) stays allowed. Scope: every tracked file outside `thirdparty/` plus any file under it that differs from the base tag (crashpad's test key ships with upstream at 10.0.6 and is not ours). New `cably/tests/secret-scan.sh`: exit 0 on the real tree and on the anon key alone; each planted secret, in a throw-away repo tagged 10.0.6, must be flagged by path (assembled from fragments at run time so the test never contains a pattern); a key block under `thirdparty/` at the base tag passes, one added after it fails. Red first: the old scan missed every plant. |
| 2026-09-04 | linux | The rendered Linux icons are now in the repository. KiCad's root `.gitignore` line 88 (`*.png`, for its generated bitmaps) silently kept the 84 PNGs `cably/linux/make-icons.sh` renders under `cably/linux/icons/hicolor` out of git (only the 12 SVGs were tracked), so the first GitHub Actions x86_64 run (actions/runs/33873772473; the 70-minute build was fine) installed no PNG app/MIME icons: identity-linux.sh 37 FAIL ("no 16x16 PNG at .../hicolor/16x16/apps/org.cably.desktop.png", "MIME type icons missing"), deb.sh "icon link dangling". Test first: `cably/tests/linux-ci.sh` (d) derives every file make-icons.sh writes (each `scalable/*/*.svg` at each `SIZES=` entry) and requires it on disk, tracked by git and matching no ignore pattern (`git check-ignore --no-index`), plus exactly 96 tracked icon files and no strays; RED (84 untracked, `.gitignore:88:*.png`, 12 tracked). Fix: `cably/linux/icons/.gitignore` with `!*.png` (a deeper negation overrides the root pattern; `git check-ignore -v` names it) and the 84 PNGs added; GREEN, 96 icon files tracked (97 with the .gitignore), so a fresh clone carries them. |
| 2026-09-04 | linux | Secret store: the 0600 file is used whenever the Secret Service cannot be used, for ANY reason. The same CI run failed the unit test (`CHECK failed at line 205: store.LastError().empty()`) and bridge.sh (e) ("secret service write failed: secret service store: The name org.freedesktop.secrets was not provided by any .service files"): GitHub's ubuntu-24.04 runner has a session bus (DBUS_SESSION_BUS_ADDRESS set) but no keyring, and `Available()` probed with `secret_service_get_sync( SECRET_SERVICE_NONE )`, which creates a GDBus proxy for org.freedesktop.secrets without error whether or not the name has an owner, so the store call was made and its error reported. Reproduced in the build container with `dbus-run-session -- <unit test>` (same CHECK, same message, 13 ms; `DBUS_SESSION_BUS_ADDRESS=unix:path=/nonexistent` already fell back, connect fails at once). `cably/src/cably_bridge_keychain.cpp` (non-Apple half): the probe now creates the proxy, sets its default timeout and opens a libsecret session (`secret_service_ensure_session_sync`, the OpenSession call libsecret would make first anyway: it D-Bus-activates an installed keyring and fails at once on a bus without one) under a watchdog thread that cancels a GCancellable after `CABLY_SECRET_SERVICE_TIMEOUT_MS` (default 5000) - the proxy timeout does not cover GDBus's connection/authentication handshake, and on a socket that listens but never answers the old probe blocked indefinitely (killed by a 20 s guard in the test); the probed `SecretService` is kept so libsecret's password calls reuse its open session, and dropped on the first call error so the next call probes afresh. `CABLY_SECRET_SERVICE::Available()` became `Available( std::string& aReason )`; the store layer writes/reads/removes the file whenever the service is unavailable or a lookup/store/clear errors, with `LastError()` empty, `Backend()` `file:<path>` and a new `Note()` saying why (empty with no bus at all); a service that answers is still preferred, `Load()` still finds a file-held session and the next `Save()` moves it into the service and removes the file, and only a stored item that is not a session is an error. Dropped rule: "a reachable service that fails is an error, never a silent fallback" - it made every Linux session without a keyring, CI included, an error (header, README, test comments updated). `cably-bridge-cli` prints `store_note=` after `store_backend=` when set. Tests, written first and RED against the old logic: `cably/tests/unit/test_cably_secret_store.cpp` - fake-layer cases (unavailable with a reason, store/lookup/clear errors -> file with the reason in Note(), recovery moves the session into the service with no file left, a garbage item still an error; the file-only cases use an unavailable fake instead of the platform backend, so they no longer depend on the machine's keyring), the real libsecret backend in this process, and in child processes (the binary re-executed with `CABLY_SECRET_STORE_TEST_CASE`): a dead bus address (4 ms), a listening-but-silent Unix socket with a 500 ms watchdog (save+load+clear+load = 4 probes, 2.0 s; the old code hung), and `dbus-run-session` with no keyring - the runner's condition - asserting the note carries "org.freedesktop.secrets" (23 ms); 175 checks. `cably/tests/bridge.sh` (e) on Linux runs the CLI round-trip under the same three bus conditions (own bus; `DBUS_SESSION_BUS_ADDRESS=unix:path=/nonexistent/...`; `dbus-run-session --`), each asserting `store_backend=file:...`, the 0600/0700 modes, the `store_note=` content and a save under 4 s; `cably/linux/run-tests.sh` requires the dbus-run-session case to have run (package dbus-daemon: in the image and on the runner). Full `cably/linux/run-tests.sh` in the build container (arm64) after `build.sh`: PASS in 146 s, 0 FAIL - test_cably_secret_store 175 checks, bridge.sh PASS (107 checks, all three secret store cases on the file with the expected notes, saves in 20-26 ms), theme.sh PASS, manager alive under Xvfb, identity-linux.sh PASS (72 checks, the PNG icons installed again), package-deb.sh + deb.sh PASS (55 checks; the install phase is the Docker host's). Also green: linux-ci.sh (incl. the new (d)), check-gpl-headers.sh, the trademark grep, and the unit test's macOS branch (4 checks). |
